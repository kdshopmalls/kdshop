<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kd.basic.admin.member.AdMemberMapper">
	<select id="list" resultType="com.kd.basic.common.dto.MemberDTO">
		select 
			mb_id,
			mb_name,
			mb_email,
			mb_pw,
			mb_zipcode,
			mb_addr, 
			mb_deaddr,
			mb_phone,
			mb_nick,
			mb_datesub,
			mb_updatedate
	 	from mb_list
	 	order by mb_datesub desc
	</select>
</mapper>